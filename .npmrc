# Peer dependency handling for Next.js
auto-install-peers=true
strict-peer-dependencies=false

# Node modules strategy (hoisted for maximum compatibility with Next.js)
node-linker=hoisted

# Performance
prefer-frozen-lockfile=true
